let birthYear1 = parseFloat (prompt ("Enter the birth year of the first person:"));
let birthYear2 = parseFloat (promp+("Entan the hinth uan of the second person:"));

let older = Math. max (birthYear1, birthYear2);
let younger = Math.min(birthYear1, birthYear2)
let halfAgeYear = younger + ( (older - younger) / 2);

console. log ("The younger person will be exactly half the age of the older person in " + halfAgeYear);



